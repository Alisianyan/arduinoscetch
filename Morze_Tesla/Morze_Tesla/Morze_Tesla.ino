  
#define BUZZER 11
 
  
void setup()
  
{  
int tochka =150;
int tire =500;
int pause= 4000;
int nyan =500; 
  // put your setup code here, to run once:
  
  pinMode(BUZZER, OUTPUT);
  //TTTTTTTT//////////////
  tone(BUZZER, 200); //Set the voltage to high and makes a noise 
  delay(tire);//Waits for 1000 milliseconds  
  noTone(BUZZER);//Sets the voltage to low and makes no noise  
  delay(nyan);//Waits for 1000 milliseconds  
  /////////////////////////////
   delay(nyan);//Waits for 1000 milliseconds 
    delay(nyan);//Waits for 1000 milliseconds 
////////////////////////////EEEEEEEEEEEEEEEE///////////////

  tone(BUZZER, 200); //Set the voltage to high and makes a noise 
  delay(tochka);//Waits for 1000 milliseconds  
  noTone(BUZZER);//Sets the voltage to low and makes no noise  
  delay(nyan);//Waits for 1000 milliseconds  

//////////////////////////////////////////
 delay(nyan);//Waits for 1000 milliseconds 
  delay(nyan);//Waits for 1000 milliseconds 
//////////////////CCCCCCCCCCCCCCCCCCCC////////////////////

 tone(BUZZER, 200); //Set the voltage to high and makes a noise 
  delay(tochka);//Waits for 1000 milliseconds  
  noTone(BUZZER);//Sets the voltage to low and makes no noise  
  delay(nyan);//Waits for 1000 milliseconds 

   tone(BUZZER, 200); //Set the voltage to high and makes a noise 
  delay(tochka);//Waits for 1000 milliseconds  
  noTone(BUZZER);//Sets the voltage to low and makes no noise  
  delay(nyan);//Waits for 1000 milliseconds 

   tone(BUZZER, 200); //Set the voltage to high and makes a noise 
  delay(tochka);//Waits for 1000 milliseconds  
  noTone(BUZZER);//Sets the voltage to low and makes no noise  
  delay(nyan);//Waits for 1000 milliseconds 

/////////////////////////////
delay(nyan);
delay(nyan);
/////////////////////////////

//////////////////LLLLLLLLLLLLLLLLLL//////////////////

tone(BUZZER, 200); //Set the voltage to high and makes a noise 
  delay(tochka);//Waits for 1000 milliseconds  
  noTone(BUZZER);//Sets the voltage to low and makes no noise  
  delay(nyan);//Waits for 1000 milliseconds 

   tone(BUZZER, 200); //Set the voltage to high and makes a noise 
  delay(tochka);//Waits for 1000 milliseconds  
  noTone(BUZZER);//Sets the voltage to low and makes no noise  
  delay(nyan);//Waits for 1000 milliseconds 


 tone(BUZZER, 200); //Set the voltage to high and makes a noise 
  delay(tire);//Waits for 1000 milliseconds  
  noTone(BUZZER);//Sets the voltage to low and makes no noise  
  delay(nyan);//Waits for 1000 milliseconds  

    tone(BUZZER, 200); //Set the voltage to high and makes a noise 
  delay(tochka);//Waits for 1000 milliseconds  
  noTone(BUZZER);//Sets the voltage to low and makes no noise  
  delay(nyan);//Waits for 1000 milliseconds 
//////////////////////////////////////
 delay(nyan);//Waits for 1000 milliseconds 
  delay(nyan);//Waits for 1000 milliseconds 





////////aaaaaaaaaaaaaaaaaaaaaaaa///////////

 tone(BUZZER, 200); //Set the voltage to high and makes a noise 
  delay(tochka);//Waits for 1000 milliseconds  
  noTone(BUZZER);//Sets the voltage to low and makes no noise  
  delay(nyan);//Waits for 1000 milliseconds 


 tone(BUZZER, 200); //Set the voltage to high and makes a noise 
  delay(tire);//Waits for 1000 milliseconds  
  noTone(BUZZER);//Sets the voltage to low and makes no noise  
  delay(nyan);//Waits for 1000 milliseconds  


 delay(nyan);//Waits for 1000 milliseconds 
 
}  
void loop()
  
{  
}
